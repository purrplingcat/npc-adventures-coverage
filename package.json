{
  "name": "npc-adventures-coverage",
  "version": "1.0.0",
  "description": "NpcAdventures localization coverage reporter",
  "author": "PurrplingCat",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development NUXT_PORT=3333 nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@mdi/font": "^5.0.45",
    "cross-env": "^5.2.0",
    "date-fns": "^2.11.0",
    "express": "^4.17.3",
    "nuxt": "^2.0.0",
    "vuetify": "^2.2.17"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.0",
    "nodemon": "^1.18.9"
  }
}
